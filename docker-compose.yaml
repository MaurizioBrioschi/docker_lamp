version: '2'
services:
  web:
    build:
      context: .
    image: ridesoft_lamp:0.2
    volumes:
      - /path/to/myproject/src:/var/www/html
    ports:
      - "80:80"
      - "443:443"
    restart: always
    networks:
      - ridesoft

  db:
    image: mysql:5.5
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: ridesoft
      MYSQL_PASSWORD: ridesoft
      MYSQL_DATABASE: ridesoft
    volumes:
      - /path/to/myproject/data/mysql:/data
    ports:
      - 3306:3306
    networks:
      ridesoft:
        aliases:
          - mysql_db
  redis:
    image: redis
    volumes:
      - /path/to/myproject/data/redis:/var/lib/redis
    networks:
      ridesoft:
        aliases:
          - redis
networks:
  ridesoft:
    driver: bridge
